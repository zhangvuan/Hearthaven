<Window x:Class="Hearthaven.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Hearthaven.Controls"
        xmlns:views="clr-namespace:Hearthaven.Views"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        mc:Ignorable="d"
        Title="炉心 Hearthaven" Width="1024" Height="768"
        Icon="/Assets/Images/Logo.ico"
        WindowStartupLocation="CenterScreen"
        FontFamily="{StaticResource FontFamily}"
        FontSize="14"
        Background="{DynamicResource BrushBg}"
        Effect="{StaticResource ShadowCard}"
        Closing="Window_Closing">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/MessageTemplates.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- ===== 左侧：侧边栏 ===== -->
        <views:SidebarView Grid.Column="0" x:Name="Sidebar" />

        <!-- ===== 分隔线 ===== -->
        <Border Grid.Column="1"
                Background="{DynamicResource BrushBorder}"
                Width="1"
                Margin="0" />

        <!-- ===== 右侧：对话区 ===== -->
        <controls:ChatAreaControl Grid.Column="2" x:Name="ChatArea" />

        <!-- ===== 系统托盘图标 ===== -->
        <tb:TaskbarIcon x:Name="TrayIcon"
                          IconSource="{StaticResource TrayIcon}"
                          ToolTipText="炉心 Hearthaven"
                          TrayLeftMouseUp="TrayIcon_LeftClick">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="设置" Click="OpenSettings_Click" />
                    <MenuItem Header="显示窗口" Click="ShowWindow_Click" />
                    <MenuItem Header="退出" Click="Exit_Click" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Grid>
</Window>
