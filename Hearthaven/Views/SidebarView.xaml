<UserControl x:Class="Hearthaven.Views.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.openxmlformats.org/expression/blend/2008"
             xmlns:vm="clr-namespace:Hearthaven.ViewModels"
             mc:Ignorable="d"
             d:DesignWidth="240"
             Width="240"
             Background="{DynamicResource BrushSidebar}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 标题栏 + 新建按钮 -->
        <Border Grid.Row="0"
                BorderBrush="{DynamicResource BrushBorder}"
                BorderThickness="0,0,0,1"
                Padding="16,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="/Assets/Images/Logo.png"
                           Width="18" Height="18"
                           Margin="0,0,6,0" />
                    <TextBlock Text="炉心"
                               Foreground="{DynamicResource BrushText}"
                               FontSize="15"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center" />
                </StackPanel>

                <Button Grid.Column="1"
                        Content="✚"
                        FontSize="16"
                        FontWeight="Bold"
                        Width="30" Height="30"
                        Background="{DynamicResource BrushHover}"
                        BorderThickness="0"
                        Foreground="{DynamicResource BrushTextSecondary}"
                        ToolTip="新建会话"
                        Command="{Binding NewSessionCommand}" />
            </Grid>
        </Border>

        <!-- 会话列表 -->
        <ListBox Grid.Row="1"
                 x:Name="SessionListBox"
                 ItemsSource="{Binding Sessions}"
                 Background="Transparent"
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 SelectionChanged="OnSelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="8,3" Cursor="Hand">
                        <!-- 背景卡片 -->
                        <Border Padding="12,10"
                                CornerRadius="10"
                                Background="Transparent">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}"
                                           Foreground="{DynamicResource BrushText}"
                                           FontSize="13"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{Binding Preview}"
                                           Foreground="{DynamicResource BrushTextSecondary}"
                                           FontSize="11"
                                           TextTrimming="CharacterEllipsis"
                                           Margin="0,2,0,0"
                                           MaxHeight="32" />
                                <TextBlock Text="{Binding UpdatedAtDisplay}"
                                           Foreground="{DynamicResource BrushTextMuted}"
                                           FontSize="10"
                                           Margin="0,2,0,0" />
                            </StackPanel>
                        </Border>

                        <!-- 删除按钮：浮动在右上角 -->
                        <Button Content="✕"
                                FontSize="12" FontWeight="Bold"
                                Width="22" Height="22"
                                Padding="0"
                                Background="{DynamicResource BrushHover}"
                                BorderThickness="0"
                                Foreground="{DynamicResource BrushTextMuted}"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                Margin="0,4,4,0"
                                ToolTip="删除此会话"
                                Command="{Binding DataContext.DeleteSessionCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                CommandParameter="{Binding}">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Opacity" Value="0" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}}" Value="True">
                                            <Setter Property="Opacity" Value="1" />
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource BrushDanger}" />
                                            <Setter Property="Background" Value="{DynamicResource BrushBubbleUser}" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="IsSelected" Value="{Binding IsSelected}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Root"
                                        Background="{TemplateBinding Background}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="8"
                                        Margin="4,1"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Root" Property="Background" Value="{DynamicResource BrushHover}" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Root" Property="Background" Value="{DynamicResource BrushBubbleUser}" />
                                        <Setter Property="BorderThickness" Value="2,0,0,0" />
                                        <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource BrushAccent}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <!-- 底部：设置按钮 -->
        <Border Grid.Row="2"
                BorderBrush="{DynamicResource BrushBorder}"
                BorderThickness="0,1,0,0"
                Padding="12,8">
            <Button Content="⚙️ 设置"
                    FontSize="12"
                    Padding="8,6"
                    Cursor="Hand"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="{DynamicResource BrushTextMuted}"
                    HorizontalAlignment="Stretch"
                    ToolTip="打开设置"
                    Click="OnSettingsClick">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="Transparent" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource BrushHover}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Border>
    </Grid>
</UserControl>
