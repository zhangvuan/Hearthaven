<UserControl x:Class="Hearthaven.Views.SettingsTabs.SettingsToolsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Hearthaven.ViewModels"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <StackPanel>
            <!-- Token 使用率 -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="工具 Token 使用率上限"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource BrushText}" />
                        <TextBlock Grid.Column="1"
                                   Text="{Binding MaxToolTokenRatio, StringFormat={}{0:P0}}"
                                   FontSize="14"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource BrushAccent}" />
                    </Grid>
                    <TextBlock Text="超过此比例时，AI 会自动总结并结束工具调用"
                               FontSize="11"
                               Foreground="{DynamicResource BrushTextMuted}"
                               Margin="0,4,0,6" />
                    <Slider Minimum="0" Maximum="1"
                            SmallChange="0.05" LargeChange="0.1"
                            Value="{Binding MaxToolTokenRatio}"
                            TickFrequency="0.25" TickPlacement="BottomRight"
                            Style="{StaticResource ModernSlider}" />
                </StackPanel>
            </Border>

            <!-- 工具开关 -->
            <Border Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="🔌 工具开关"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource BrushText}"
                               Margin="0,0,0,6" />
                    <TextBlock Text="关闭后可禁用不需要的工具，减少 Token 消耗"
                               FontSize="11"
                               Foreground="{DynamicResource BrushTextMuted}"
                               Margin="0,0,0,8" />
                    <ItemsControl ItemsSource="{Binding Tools}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:ToolSwitchItem}">
                                <CheckBox IsChecked="{Binding IsEnabled}"
                                          Style="{StaticResource ConfigCheckBox}"
                                          Margin="0,4,0,4">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding DisplayName}"
                                                   FontWeight="SemiBold"
                                                   Margin="0,0,6,0" />
                                        <TextBlock Text="{Binding Description}"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource BrushTextMuted}" />
                                    </StackPanel>
                                </CheckBox>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
