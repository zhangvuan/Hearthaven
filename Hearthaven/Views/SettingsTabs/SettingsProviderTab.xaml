<UserControl x:Class="Hearthaven.Views.SettingsTabs.SettingsProviderTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Hearthaven.ViewModels"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 模型列表卡片 -->
        <Border Grid.Row="0" Style="{StaticResource CardBorder}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <!-- 卡片标题 -->
                <TextBlock Grid.Row="0"
                           Text="AI 模型"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource BrushText}"
                           Margin="0,0,0,10" />

                <!-- 模型列表 -->
                <ListBox Grid.Row="1"
                         ItemsSource="{Binding Models}"
                         SelectedItem="{Binding SelectedModel}"
                         BorderThickness="1"
                         BorderBrush="{DynamicResource BrushBorder}"
                         Background="Transparent"
                         MinHeight="120"
                         MaxHeight="200">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Cursor" Value="Hand" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Background" Value="Transparent" />
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource BrushBubbleUser}" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource BrushHover}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <!-- 模型操作按钮 -->
                <StackPanel Grid.Row="2"
                            Orientation="Horizontal"
                            Margin="0,10,0,0">
                    <Button Content="＋ 添加"
                            Style="{StaticResource TextButton}"
                            Command="{Binding AddModelCommand}" />
                    <Button Content="✏️ 编辑"
                            Style="{StaticResource TextButton}"
                            Command="{Binding EditModelCommand}"
                            CommandParameter="{Binding SelectedModel}" />
                    <Button Content="🗑 删除"
                            Style="{StaticResource TextButton}"
                            Command="{Binding DeleteModelCommand}"
                            CommandParameter="{Binding SelectedModel}" />
                    <Button Content="⭐ 设为默认"
                            Style="{StaticResource TextButton}"
                            Command="{Binding SetDefaultModelCommand}"
                            CommandParameter="{Binding SelectedModel}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- 默认模型提示 -->
        <Border Grid.Row="1"
                Background="{DynamicResource BrushBubbleUser}"
                BorderThickness="1"
                BorderBrush="{DynamicResource BrushBorder}"
                CornerRadius="10"
                Padding="14,10"
                Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📌"
                           FontSize="13"
                           VerticalAlignment="Center"
                           Margin="0,0,8,0" />
                <TextBlock FontSize="12"
                           Foreground="{DynamicResource BrushTextSecondary}"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis">
                    <Run Text="当前默认模型：" />
                    <Run Text="{Binding DefaultModelName, FallbackValue='(无)'}"
                         FontWeight="SemiBold"
                         Foreground="{DynamicResource BrushText}" />
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
