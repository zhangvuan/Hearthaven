<UserControl x:Class="Hearthaven.Views.SettingsTabs.SettingsUiTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Border Style="{StaticResource CardBorder}"
            VerticalAlignment="Top">
        <StackPanel>
            <TextBlock Text="🎨 显示设置"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource BrushText}"
                       Margin="0,0,0,10" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="主题" Style="{StaticResource FieldLabel}" />
                    <ComboBox ItemsSource="{Binding ThemeOptions}"
                              DisplayMemberPath="Label"
                              SelectedValuePath="Value"
                              SelectedValue="{Binding Theme}"
                              Style="{StaticResource FieldComboBox}"
                              Width="160" HorizontalAlignment="Left" />
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="正文字体大小" Style="{StaticResource FieldLabel}" />
                    <TextBox Text="{Binding FontSize, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FieldInputNarrow}" />
                </StackPanel>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
